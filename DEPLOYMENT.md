# iPhone 直向網頁試玩版

## 本次交付

`builds/iphone-web/` 是完整網站目錄，`index.html` 是入口。`builds/iphone-web-*.zip` 是同內容的部署壓縮包；不是 iOS 安裝檔，不能將 ZIP 或 HTML 傳到 iPhone 後直接點開遊玩。

本版保留四課教學、家園修練、正常大地圖與副本流程。主選單自由沙盒、家園各區十房／二十房試玩捷徑及竹海場景測試入口已移除；開發測試腳本留在原專案，不放進網頁資源包。

## 手機操作與字型

- 手機／網頁預設 480 × 900 直向布局；桌面可由標題畫面切換預覽。
- 敵我氣血 HUD 半透明疊在戰場上，底部為技能快捷開關、手動／自動回合及四分頁。
- 技能快捷列可左右滑動。棋盤為全寬五乘五，下方放物品欄；節點設定使用獨立分頁，這次尚未改成底部抽屜。
- 角色面板由「更多 → 角色狀態」開啟；家園與探索也保留角色按鈕。
- 標題使用 Noto Serif CJK TC SemiBold，操作與數值使用 Noto Sans CJK TC Medium；字型隨遊戲打包，附 SIL OFL 授權。
- 網頁外框使用安全區，限制像素倍率，手機瀏覽器工具列改變高度時重新配置；橫放保留直向比例，不強制要求瀏覽器鎖定方向。

## 分享給朋友

1. 選擇支援靜態網站與 HTTPS 的主機。
2. 解壓部署 ZIP，把全部檔案以原檔名上傳至同一個網站目錄，不要只上傳 `index.html`。
3. `.wasm` 使用 `application/wasm`，`.js` 使用 JavaScript MIME；`.pck` 可用 `application/octet-stream`。建議啟用 gzip／Brotli 傳輸壓縮。
4. 先拿 iPhone 在一般模式 Safari 開啟 HTTPS 網址，完成教學、進家園、進副本、出招並重新載入驗證存檔。
5. 再把同一網址傳給朋友。聊天 App 內建瀏覽器若不穩定，請選「在 Safari 開啟」。

本次沒有公開上傳、綁定網域或產生外網分享連結。啟動畫面需點「進入遊戲」，並提供載入進度及失敗重試。

## 存檔與限制

進度保存在此網站／此瀏覽器的 IndexedDB，不是雲端存檔。請勿使用私密瀏覽；清除網站資料、換網域或換裝置可能無法接續原進度。即使加入主畫面，也不保證資料永不被系統清除。

使用 Godot 4.6.2 單執行緒 Web 匯出，無須 SharedArrayBuffer 或 COOP／COEP 標頭。PWA 離線快取未啟用。沒有帳號、分析追蹤、付款功能或伺服器端遊戲邏輯。

生成的美術概念圖不是遊戲實際場景；本次沿用現有低多邊形資產及演出，不包含概念圖中的完整角色動畫。

## 重建

在專案根目錄執行 `./tools/export_iphone_web.ps1`。需要工作區內 Godot 4.6.2、`tools/web_templates/` 的官方單執行緒模板，以及 `assets/fonts/` 字型。

本機測試需 HTTP 伺服器，而非直接開檔。測試時使用 `127.0.0.1` 綁定即可；本機網址不能直接傳給朋友在其他裝置使用。

## 驗證範圍

- `tests/test_portrait_ui.gd`：直向選單、移除測試入口、正常世界／探索、HUD 疊圖、棋盤邊界、角色面板、戰鬥結算及存讀。
- `tests/test_tutorial_guide.gd -- --portrait-ui`：四課教學完成到家園，高光、設定、存讀及演出保護。
- `tests/test_combat_presentation.gd`：桌面三種播放方式結算一致。
- `tests/test_web_pack.gd`：載入已匯出的資源包，確認六區場景與字型齊全，測試／報告／工具／Blender 原稿目錄未打包。
- 本機 Chromium 瀏覽器 390 × 844：實際載入匯出檔、棋盤放置、前兩課與技能出招；重新載入後可續接第二課完成狀態，未觀察到瀏覽器錯誤。

尚未使用真實 iPhone／Safari 驗證觸控、軟鍵盤、音量／靜音行為、記憶體、溫度與 FPS；不能把桌面瀏覽器測試視為實機相容性保證。
